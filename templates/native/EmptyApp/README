
Purpose:
        webOS games app template.
        EmptyApp 

Building:
        mkdir BUILD
        cd BUILD
        cmake ..
        make
        cp ../res ../pkg/
        cd ../pkg

Generating app & icon:
        ares-generate . -t webappinfo -f
        ares-generate . -t webicon -f
        change "index.html" in "main" of appinfo.json to "EmptyApp".
        change "web" in "type" of appinfo.json to "native_game".

Packaging and installing:
        ares-package .
        ares-install your_package_name.ipk -d your_target


Testing:
        just launch


Bugs:

